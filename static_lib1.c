#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
/**
 * nums - checks if the numbers are integers
 * @num1: the first number
 * @num2: the second number
 *
 * Return: 98
 */
/*int nums(int num1, int num2)
{
        if (num1 < 48 || num1 > 57)
        {
                printf("Error\n");
                return (98);
        }
        if (num2 < 48 || num2 > 57)
        {
                printf("Error\n");
                return (98);
        }
}
/**
 * multiply - multiplys two numbers
 * @num1: first number
 * @num2: second number
 *
 * Return: num1 * num2
 */
/*int multiply(int num1, int num2)
{
        return (num1 * num2);
}
/**
 * main - prints the multiple of two numbers
 * @ac: the number of arguements
 * @av: the array of size ac
 *
 * Return: 0
 */
/*int main(int ac, char *av[])
{
        int result;

        (void)ac;
        if (ac != 3)
        {
                printf("Error\n");
                return (98);
        }
      z
        checking if num1 and num2 are integers
        if (av[1] == (int)av[1] && av[2] == (int)av[2])
        {
                result = multiply(num1, num2);
                printf("%d\n", result);
        }
        else
        {
                printf("Error\n");
                return (98);
        }
        return (0);
}*/